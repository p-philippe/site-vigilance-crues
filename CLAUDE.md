# Vigilance 22

Les conventions de ce dépôt sont dans [AGENTS.md](AGENTS.md).

**Règle principale : ne jamais éditer `public_html/index.html`** — fichier généré par `build.py` depuis `index.html` + `src/*.js`.
